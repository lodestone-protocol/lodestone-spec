# 真标题
<!-- mddag: {"id":"real-01","status":"draft"} -->
  # 缩进一级标题（1-3 空格，正文，不切分）
> # 引用块内标题（正文，不切分）

```text
# 围栏内标题（正文，不切分）
```

# 缩进代码块节点
<!-- mddag: {"id":"indented-01","status":"draft"} -->
    # 四空格缩进标题（缩进代码块，正文，不切分）
    # 同块内第二行
末行。
